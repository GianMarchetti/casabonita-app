// import Stripe from "stripe"

// export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
//   apiVersion: "2022-11-15",
// })

// const mercadopago = require("mercadopago");

// const mp = mercadopago.configure({
//   access_token: process.env.NEXT_PUBLIC_MP_TOKEN!,
// })

// export default mp





// const client = new MercadoPagoConfig({ accessToken: 'NEXT_PUBLIC_MP_TOKEN', options: { timeout: 5000, idempotencyKey: 'abc' } });

// const payment = New MercadoPagoPayment(client)

// const body = {
//   transactio_amount: 12.34,
//   payment_method_id: '<PAYMENT_METHOD_ID>',
//   payer: {
//     email: '<EMAIL>'
//   }
// }

// payment.create({body}).then(console.log).catch(console.log)
